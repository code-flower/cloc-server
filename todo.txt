
- figure out why cloc is ignoring certain files

- can probably delete cloc data after analysis (also change path)

- should pull instead of clone if repo is already there

- need a way to cancel a clone in the UI

- change 'root' to '/'

- checkout http://graus.nu/d3/ for zoom function

- added repo doesn't always show up in current repo dropdown

- deal with case where all repos are deleted

- use websockets instead of event source

- modularize the server code

- add way to track private repos

- make codeflower colors track the file types

- implement update button
  - just delete the repo in the DB followed by a new clone
